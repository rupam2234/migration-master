import { Main } from "../index";

export const metadata = {
  title:
    "Migration Master | Shopify to WordPress & WordPress to Shopify Migration Tool",
  description:
    "Migrate products, pages, blog posts, and media between Shopify and WordPress with Migration Master. Export and import your content quickly with a reliable migration utility.",
};

export default function Home() {
  return <Main />;
}
